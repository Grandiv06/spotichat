services:
  mongo:
    image: mongo:7
    container_name: spotichat-mongo
    ports:
      - "27017:27017"
    volumes:
      - spotichat-mongo-data:/data/db
    restart: unless-stopped

volumes:
  spotichat-mongo-data:
